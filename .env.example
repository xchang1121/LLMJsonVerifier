# Optional gateway authentication. Leave empty only for a trusted local deployment.
LLMJV_API_KEY=
# Shared by the private vLLM server and gateway; distinct from the gateway key.
LLMJV_VLLM_API_KEY=
# Only needed when Hugging Face access requires authentication.
HF_TOKEN=
